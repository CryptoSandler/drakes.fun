# Reference images

**Mood references only. Nothing here is a production asset and nothing here
ships.**

- `hero-mood.*` — the owner's hero reference for the Drakes register: black
  dragon, street, chain, "already made it". **AI-generated mood reference.** It
  settles the look in one glance for the illustrator brief. It is not a layer,
  not traced, not composited, and never delivered.

Anything in this directory that ever becomes an asset stops being a reference
and gets its own licence trail (CLAUDE.md, reused assets are how two pseudonyms
get linked).
